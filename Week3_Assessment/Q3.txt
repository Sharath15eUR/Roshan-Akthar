3. Guess the output for the following program.

#include <stdio.h>

#include <unistd.h>

int main()

{

if (fork() && (!fork())) {

if (fork() || fork()) {

fork();

}

}

printf("2");

return 0;

}


OUTPUT 1: 2
OUTPUT 2: 2 2
OUTPUT 3: 2

The output of this program can vary each time it's run due to the multiple fork() calls and the non-deterministic nature of process scheduling.

